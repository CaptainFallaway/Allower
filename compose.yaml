services:
    allower:
        image: allower:latest
        container_name: allower
        network_mode: host
        environment:
            IPINFO_TOKEN: ${IPINFO_TOKEN:-}
            LOG_PRETTY: false
            LOG_LEVEL: "info"
        volumes:
            - ./test-data:/persist
